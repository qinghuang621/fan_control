fan_control已经是一个能够烧录至C板的完整程序。running是曾经烧录到正点原子探索者板的程序，实现了通过rs485连接上位机写入数据通过can口下发给四个达妙电机。目前我需要把running的程序功能移植到C板中，具体可见D:\stm32\running\RM_C_Board_Template\板子迁移技术文档.md，但是RM_C_Board_Template烧录失败了，应该是型号不匹配，所以直接移植到fan_control中。
同时fan_control已经测试过，可以通过上位机虚拟串口控制pwm输出占空比从而控制电机输出。但是现在我要改成pwm接口的5 6 7向外输出占空比方波控制电机（目前fan_control中是pwm接口1输出），pwm接口的1 2 3 4用于读取电机的脉冲反馈，配置 4 路定时器输入捕获，接收 4 路 FG 转速反馈，全部只用 STM32 内部上拉，白线直插 C 板4 路PWM‑S 引脚。
原先的runing中通过寄存器下发读取达妙电机信号和速度，现在我希望也能通过modbus协议上位机连接串口后通过寄存器下发pwm占空比，也能读取反馈脉冲后读取至寄存器。