1. credit-limit-inquiry 已经是一个能够烧录至 C 板的完整程序。running 是曾经烧录到正点原子探索者板的程序，实现了通过 RS485 连接上位机写入数据、通过 CAN 口下发给四个达妙电机。目前需要把 running 的程序功能移植到 C 板中，running具体参数在"D:\stm32\running\README.md"中，移植想法在"D:\stm32\running\板子迁移技术文档.md“。

## 已执行步骤与当前进展

1. 先确认了当前项目的真实 PWM 资源和工程结构。
   - 现有 firmware 里最初只用到 TIM1_CH2 与 TIM8_CH1；FG 反馈和 PWM 输出均不足。
   - 目标是让 PWM1~PWM4 作为四路 FG 输入捕获，并让 PWM5~PWM7 作为三路同频同占空比输出。

2. 按硬件图和板卡引脚说明修正了 PWM/定时器映射。
   - PWM1~PWM4：TIM1_CH1/CH2/CH3/CH4，对应 PE9 / PE11 / PE13 / PE14。
   - PWM5~PWM7：TIM8_CH1/CH2/CH3，对应 PC6 / PI6 / PI7。
   - 其中 TIM8_CH2/CH3 的引脚原先错误地按 PC7/PC8 处理，已依据板卡图修正为 PI6/PI7。

3. 在软件中补齐了四路脉冲反馈测量。
   - `bsp/boards/bsp_pulse.c` 现在支持四路 FG 计数、500 ms 窗口测频、RPM 计算和累计脉冲统计。
   - 通过 `pulse_get_freq_hz()` / `pulse_get_rpm()` / `pulse_get_pulse_count()` 对外提供各通道数据。
   - 代码中以 `GPIO_PULLUP` 方式配置了 FG 输入的内部上拉，适用于 FG/开集电极式反馈信号，前提是输入电平需兼容 3.3V。

4. 实现了三路 PWM 输出同步控制。
   - `bsp/boards/bsp_fric.c` 中 `fric_apply_ccr()` 现在将 TIM8 的 CH1/CH2/CH3 统一写入相同 CCR。
   - 这使 PWM5~PWM7 输出相同的 20 kHz 频率和占空比，符合 “同一控制命令同时驱动三路” 的要求。

5. 扩展了状态输出与工程文档。
   - `Src/usbd_cdc_if.c` 已增加四路 FG 状态的 USB CDC 输出字段。
   - `readme.md` 已重写为框架说明文档，`计划.md` 作为迁移与实施计划保留。

6. 当前状态判断。
   - 已经完成了 C 板上 “四路 FG 反馈 + 三路同频 PWM 输出” 的底层驱动、定时器与 GPIO 对齐。
   - 工程可编译，当前模块和引脚配置已符合设计要求。
   - 但这还不是 running 项目的最终 RS485/Modbus/CAN 迁移完成版；当前仍属于“功能适配与引脚/定时器修正”阶段。

## 下一步实施计划

1. 读取并对照 `D:\stm32\running\README.md` 与 `D:\stm32\running\板子迁移技术文档.md`，确认 running 项目的最终寄存器映射、USART2/RS485 和 CAN 协议用法。
2. 把 running 里的 Modbus RTU 从站逻辑移植到当前项目，切换从 USB 虚拟串口模式到真实串口/RS485 通信。
3. 统一寄存器定义与控制字节，确保上位机可通过 Modbus 读写与运行项目一致的寄存器。
4. 把 CAN 发送、状态解析和电机控制任务整合进当前工程，确保目标平台仍能按“达妙电机 + CAN 控制”工作。
5. 在实际硬件上完成端口联调：FG 输入采样、PWM 输出、RS485 通讯、CAN 控制和状态反馈。

### 硬件连接对应关系（必须保持）
- PWM1 / TIM1_CH1 -> 风机编号 1 的 FG 反馈。
- PWM5 / TIM8_CH1 -> 风机编号 1 的 PWM 控制输出。
- PWM2 / TIM1_CH2 -> 风机编号 2 的 FG 反馈。
- PWM6 / TIM8_CH2 -> 风机编号 2 的 PWM 控制输出。
- PWM3 / TIM1_CH3 -> 风机编号 3 的 FG 反馈。
- PWM7 / TIM8_CH3 -> 风机编号 3 与风机编号 4 的共享 PWM 控制输出。
- PWM4 / TIM1_CH4 -> 风机编号 4 的 FG 反馈。

也就是说：
- 1/5 是风机 1。
- 2/6 是风机 2。
- 3/7 是风机 3。
- 4/7 是风机 4。
- 7 口同时驱动风机 3 和风机 4，两个风机共用同一路 PWM 输出。

当前阶段的目标是：先把底层硬件映射和信号输入输出调整到位，再按 running 的通信协议和状态机完成最终移植。

### 2026-09-08 补充进展
- 已在当前工程中加入 RS485 端口初始化（USART2 + PA2/PA3 + PG8），并在主循环中接入 Modbus RTU 轮询。
- 新增 `bsp/boards/bsp_modbus.c/h`，实现了最小可用的 Modbus RTU 从站：支持 0x03/0x04 读寄存器、0x06 单寄存器写、0x10 多寄存器写，并保留了 256 个 holding register 空间。
- 参考 running 项目 `README.md` 与 `板子迁移技术文档.md` 的地址规划，已将当前寄存器逐段对齐为：
  - `0x0000~0x003F`：控制/命令区（当前映射为 4 个风机占空比控制）
  - `0x0040~0x008F`：CAN 状态区（当前保留为只读状态区，禁止 Modbus 写入）
  - `0x0090~0x00BF`：参数区（保留 UART_CFG、SLAVE_ADDR 等配置寄存器）
  - `0x00C0~0x00FF`：保留区
- 按 running 版本的语义，状态区已按每个风机 10 个寄存器的方式建模，并用 FGC 反馈/占空比/脉冲计数填充对应状态字段；参数区已定义 `0x0090`（UART_CFG）和 `0x0091`（SLAVE_ADDR）默认值，`SLAVE_ADDR` 非法值会回退为 1。
- 代码中已增加 `is_can_status_region()` / `is_param_region()` 语义检查，Modbus 对 `0x0040~0x008F` 状态区写入会被忽略，以符合 running 项目的“状态只读 / 参数写入”权限管理。
- 工程已通过 CMake 构建验证，说明当前 RS485 Modbus 的初始化、寄存器分区和编译集成无明显错误。
- 下一步将继续按 running 的寄存器详细布局推进：把 4 个电机状态块的字段（ERR/POS/VEL/T/T_MOS/T_Rotor）按规范继续补全，并为参数区增加真正的 Flash 断电保持落盘逻辑。
- 2026-09-08 继续推进：已按 running 语义把每个状态块改成 `ERR + reserved + POS(float32) + VEL(float32) + T(float32) + T_MOS + T_Rotor` 的 10 寄存器布局，并通过 `write_float32_le()` 把 32 位浮点值拆成高低 16 位寄存器，保证 Modbus 读取时保持与 running 相容的字段排列。
- 2026-09-08 进一步对接：已在 `bsp_modbus.c` 中补上参数区断电保持思路：`0x0090~0x00BF` 可写参数按 A/B 双区写入 Flash，并在主循环里延迟 2 秒提交，具备 running 版的“参数掉电保持”雏形。
- 2026-09-08 进度评估：当前工程已完成“硬件映射 + Modbus 读写框架 + 风机状态区布局 + 参数区掉电保持雏形”的主体工作；风机的占空比写入在代码层已接通到 PWM 输出，状态区读取已实现到寄存器级数据回填。当前仍未移植 running 中的 FreeRTOS 多任务机制；工程依然采用裸 `main()` 主循环，后续需要按 running 的任务拆分重新补齐。与此同时，C 板的最终用途已定为：作为 EBS-P300 侧 RS485 设备节点，通过 Modbus RTU 连接上层 ROS2 分节点；因此 MCU 只做设备层控制与状态回传，ROS2 在上位机侧负责协议拆分、状态发布和策略联动。剩余关键项仍是：真实硬件联调、CAN 电机状态语义完善、Flash 参数断电保持细节收口、FreeRTOS 任务迁移收口和 ROS2 节点端最终集成测试，这五者构成后续最后的移植闭环。

- 2026-09-08 收尾补充：已修正 Modbus 从站站号的运行时配置逻辑，`REG_PARAM_SLAVE_ADDR`（0x0091）现在会在请求校验时作为实际地址生效，默认 1 号站支持常规 EBS-P300 访问；这样可以在 ROS2 侧按节点/总线地址分开管理不同设备。
- 2026-09-08 继续收尾：已按 running 的电机协议语义把 CAN 电机控制区切回到 `0x0000~0x0019`，并将风机占空比控制区整体后移至 `0x0100~0x0103`，避免与运行项目保留的电机控制寄存器冲突；这意味着电机地址保持不变，风机地址按需要迁移，不破坏 running 的 motor_id / wheel speed / vx-vy-wz 定义。
- 2026-09-08 进一步补齐：当前工程增加了 CAN 1 的底层初始化与发送逻辑，支持通过 `CAN1` 发送电机使能、清错和速度命令，并在主循环中消费 CAN 接收 FIFO，实现对 4 个达妙电机的状态回读与速度反馈更新；`vx/vy/wz` 维持按运行项目的运动学分解计算轮速，写回 `0x000A~0x0011` 的四轮目标速度寄存器。
- 2026-09-08 验证：已使用项目内 CMake 方式编译验证，命令为 `cmake --preset Debug` 和 `cmake --build build/Debug -- -j 8`，当前工程已成功构建并产出 `pwm_snail.elf` / HEX / BIN，可继续进入真实联调阶段。此时仍需保持“风机控制”和“电机控制”两套功能分离：风机走 PWM/FG；电机走 CAN/运动学，确保两者地址和功能边界不互相覆盖。

## 2026-09-08 代码审查与缺陷修复记录

对已完成的移植做了逐项代码审查，定位出 7 个问题，按“一个一个来”的顺序逐个修复。以下为修复过程。

### 问题 1：FreeRTOS 无时基，任务永久挂起（已修复）

- **现象**：`Src/stm32f4xx_it.c` 的 `SysTick_Handler` 只调用了 `HAL_IncTick()`，工程内没有任何地方调用 `xPortSysTickHandler()`。`vTaskStartScheduler()` 之后，任务第一次执行到 `vTaskDelay()` 就被挂起，而 `xTickCount` 永远不增长，永远等不到唤醒，调度器只能跑 idle，板子表现为死机。
- **证据**：map 文件中 `.text.xPortSysTickHandler` 地址为 `0x00000000`，已被 `--gc-sections` 当作无人引用丢弃。
- **易误解点**：`HAL_GetTick()` 是正常的（SysTick 确实在跑、uwTick 在涨），但没用，任务根本没机会再被调度到。
- **与 running 的差异**：running 把两个时基分开（CubeMX 勾 FreeRTOS 后的标准做法）——SysTick 归 FreeRTOS，HAL 时基改用 TIM6；且 `SysTick_Handler` 由 `cmsis_os2.c` 提供，CubeMX 在 FreeRTOS 工程里不生成它。C 板工程没有 `stm32f4xx_hal_timebase_tim.c`，也没编 `cmsis_os2.c`（用的是 `xTaskCreate`/`vTaskDelay` 原生 API），所以 SysTick 中断里没人喂 FreeRTOS。
- **修复（方案 A，与 running 一致）**：
  1. 新增 `Src/stm32f4xx_hal_timebase_tim.c`（照搬 running 同名文件），用 TIM6 提供 HAL 的 1ms 时基，强定义 `HAL_InitTick` / `HAL_SuspendTick` / `HAL_ResumeTick`。APB1=42MHz 时 TIM6CLK=84MHz，PSC=83 → 1MHz，ARR=999 → 1kHz。
  2. `Src/stm32f4xx_it.c`：`SysTick_Handler` 改为读 `SysTick->CTRL` 清溢出标志，并判断调度器已启动后调 `xPortSysTickHandler()`（cmsis_os2.c 的标准写法）；新增 `TIM6_DAC_IRQHandler()` → `HAL_TIM_IRQHandler(&htim6)`。
  3. `Src/main.c`：新增 `HAL_TIM_PeriodElapsedCallback()`，判定 TIM6 后调 `HAL_IncTick()`。
  4. `Inc/stm32f4xx_hal_conf.h`：`TICK_INT_PRIORITY` 由 `0` 改为 `15U`，与 running 对齐。
  5. `cmake/stm32cubemx/CMakeLists.txt`：新文件加入 `MX_Application_Src`。
- **踩到的坑**：C 板 HAL 里的 `uwTickPrio` 只在 `stm32f4xx_hal.c` 定义、未在头文件 extern（running 在 `stm32f4xx_hal.h` 有），直接编译报错。未改动 Drivers 目录，改为在新文件内加 `extern uint32_t uwTickPrio;` 声明。
- **验证**：`.text.xPortSysTickHandler` 由 0x0 变为 `0x08008bb8`，`hal.c` 的弱 `HAL_InitTick` 已被丢弃，反汇编确认 `SysTick_Handler → xTaskGetSchedulerState → xPortSysTickHandler`。编译 0 错误 0 警告。
- **前提确认**：TIM6 无冲突（全工程无 TIM6 引用、DAC 模块未启用、TIM6 不带 IO 复用；已占用定时器只有 TIM1/TIM8/TIM5）。

### 问题 2：运动学解算限幅吃掉符号（已修复）

- **背景澄清**：底盘是**正交全向轮**（LF/RR 为 X 轮只吃 vx，RF/LR 为 Y 轮只吃 vy），**不是麦克纳姆轮**。公式本身与 running `Core/Src/kinematics.c` 逐行一致，无需改动。注意 `running\RM_C_Board_Template\...\chassis_kinematics.c` 的公式与 running 主体不一致（旋转项系数 1.0 而非 1.5），不能当参考。
- **缺陷 1（严重）**：限幅时求最大值用了就地取绝对值 `if (vals[i] < 0) vals[i] = -vals[i];`，随后把 `vals` 原样写回保持寄存器 → 四轮速度**恒为非负**，电机永不反转，后退与反向自转全部出错。running 的 `prvScaleAll` 用 `fabsf()` 存局部副本。改为用局部变量求 max，仅用于算比例系数。
- **缺陷 2**：解算触发范围 `0x0014~0x0018` 漏了 `0x0019`（wz 低字）。上位机一次写 6 个寄存器时最后一个字不触发解算，wz 会用“新高字 + 旧低字”。
- **缺陷 3**：缺少通信丢失保护与 NaN 防护。按 running 补上 500ms 心跳超时强制停车 + `isfinite` 等价检查。
- **架构对齐**：解算时机从“写寄存器时立即触发”改为“`motor_can_tick()` 每 10ms 发速度前调用一次”，与 running 的 CanTask 一致，保证 vx/vy/wz 来自同一时刻快照。
- **验证**：三方数值对比（running / 旧 C 板 / 新 C 板），10 组工况 × 4 轮 = 40 项，旧实现 16/40 不一致（全是符号错误），新实现 0/40。

### 问题 3：CAN1 波特率错误 + 引脚错误 + 缺少 HAL CAN 驱动（已修复）

- **缺陷 1（波特率）**：`bsp/boards/bsp_modbus.c` 的 `MX_CAN1_Init()` 中 `CAN1->BTR = (3<<16)|(13<<8)|(2<<4)`，移位量全错。`CAN_BTR` 真实位域为 `SJW[25:24]`、`TS2[22:20]`、`TS1[19:16]`、`BRP[9:0]`，13 落入保留位、2 把 BRP 撑成 289。实测生效 `BRP=289 / TS1=4TQ / TS2=1TQ`，波特率 = 42MHz/(289×6) ≈ **24.2 kbps**，达妙要的是 1 Mbps，**差 41.3 倍**。
- **缺陷 2（引脚）**：原代码把 PA11/PA12 配成 CAN1，但那两个脚是 **USB_OTG_FS_DM/DP**。经用户查 RoboMaster C 板用户手册确认，**CAN1 实际引脚为 PD0 (RX) / PD1 (TX)，AF9**。PD0/PD1 在本工程无任何其它用途。
- **缺陷 3（驱动缺失）**：C 板的 HAL 是裁剪过的（Drivers 下只有 18 个 .c），**没有 `stm32f4xx_hal_can.c`**，直接照搬 running 的 `MX_CAN1_Init()` 会报 implicit declaration。
- **修复**：
  1. 从 running 复制 `stm32f4xx_hal_can.c`（2466 行）与 `stm32f4xx_hal_can.h` 到 C 板 Drivers 对应目录。
  2. `Inc/stm32f4xx_hal_conf.h`：开启 `HAL_CAN_MODULE_ENABLED`，注释 `HAL_PCD_MODULE_ENABLED`。
  3. `bsp/boards/bsp_modbus.c`：引脚改 `GPIOD` 的 `GPIO_PIN_0|GPIO_PIN_1` + `GPIO_AF9_CAN1`，时钟改 `__HAL_RCC_GPIOD_CLK_ENABLE()`；BTR 改为 `0x001A0002`（SJW=1TQ / TS2=2TQ / TS1=11TQ / BRP=2），位时间 14TQ，42MHz/(3×14) = **1.000 Mbps**，采样点 85.7%，与 running（PSC=3 / BS1=11TQ / BS2=2TQ / SJW=1TQ）完全一致；MCR 增加 `CAN_MCR_NART`（禁止自动重传，避免总线无应答时一直重发占满三个邮箱、叠加发送函数 while 死等把 ModbusTask 挂死）。
  4. `Src/main.c`：删除 `MX_USB_DEVICE_Init()` 调用与 `usb_device.h` 包含。
  5. `Src/stm32f4xx_it.c`：删除 `OTG_FS_IRQHandler` 与 `hpcd_USB_OTG_FS` 外部声明。
  6. `cmake/stm32cubemx/CMakeLists.txt`：移除 4 个 USB 应用源（usb_device/usbd_conf/usbd_desc/usbd_cdc_if）、USB 中间件库与 USB include 路径；加入 `stm32f4xx_hal_can.c` 到 `STM32_Drivers_Src`；`MX_LINK_LIBS` 移除 `USB_Device_Library`。
- **决策记录（用户已确认）**：
  - **USB CDC 不再保留**，虚拟串口调试功能整体下线。
  - **NVIC 暂不开**：running 为 CAN1_RX0/TX 开了 IRQ priority 5，C 板维持现有“裸寄存器 + 轮询 FIFO”风格，暂不加中断。
  - **GPIO 用 NOPULL 而非 running 的 PULLUP**：达妙电机侧收发器/终端电阻一般自带偏置，板上拉易与总线既有上下拉打架。
- **验证**：编译 0 错误 0 警告。FLASH 由 56 KB 降至 **31.7 KB**，RAM 由 35 KB 降至 27.9 KB（USB 库移除）。map 中已见 `HAL_CAN_Init` 等符号，USB 符号仅剩启动文件的默认弱向量（无害）。PD0/PD1 无冲突，PA11/PA12 已在 gpio.c 中无配置。
- **遗留**：`Src/usb_device.c`、`usbd_conf.c`、`usbd_desc.c`、`usbd_cdc_if.c` 四个文件仍留在磁盘但已不参与编译（其中 `usbd_cdc_if.c` 含 FG 状态 USB 输出代码，暂时保留备用）。如需彻底清理可另行删除。

### 问题 4：CAN 发送死等空转（已修复，2026-09-09）

- **现象**：`motor_can_send_frame()` 用 `while ((TSR&TME0)==0 && (TSR&TME1)==0 && (TSR&TME2)==0) {}` 死等空闲发送邮箱，**无超时、不让出 CPU、不中止挂起帧、不看发送结果**，函数固定 `return 1`。
- **严重度来源**：该函数运行在 `ModbusTask`（`tskIDLE_PRIORITY + 4`，全系统最高优先级）。一旦卡住，FanTask(+3)、PulseTask(+2)、LedTask(+1) 全部饿死：RS485 不回应、风机斜坡停、LED 停。上层表现为"设备掉线"。且工程**未启用看门狗**（`HAL_IWDG_MODULE_ENABLED` / `HAL_WWDG_MODULE_ENABLED` 均为注释状态），卡死无法自愈。
- **触发场景**：电机未上电 / CAN 线断 / H-L 反接 / 缺少终端电阻 / 波特率仍有偏差 → 每帧都收不到 ACK → TEC 每次 +8 → 按 10ms×4 帧的速率约 80ms 撑满 TEC → **CAN 进入 bus-off**，pending 帧不释放，TME 始终为 0。
- **NART 只是缓解**：前一轮在 MCR 加了 `CAN_MCR_NART`（禁止自动重传，发一次失败即释放邮箱），堵掉了"无限重传占满邮箱"这条最常见的死路，但四个缺陷仍在：等待无上限、空转不让出 CPU、没有 abort 逃生通道、不看 TXOK/ALST/TERR。
- **与 running 的差异**：running 走相反路线——`hcan1.Init.AutoRetransmission = ENABLE`（即 NART=0，保留自动重传），在 `CanTask_Send()`（`Core/Src/freertos.c:288`）里靠"超时 + 强制中止"兜底：邮箱满时 `osDelay(1)` 最多 5 次（**会让出 CPU**），仍满则对三个 pending 邮箱调 `HAL_CAN_AbortTxRequest()`（写 `TSR` 的 `ABRQx` 位），最后返回 `HAL_CAN_AddTxMessage()` 的真实状态。注释中说明这是照搬正点原子裸机 `can_send_msg` 的超时 abort 行为。另外 running 有独立的 CanTask，CAN 异常不会拖垮其余任务。
- **修复（用户选择"对齐 running"）**：
  1. `MCR` 去掉 `CAN_MCR_NART`，回到自动重传（`CAN_MCR_ABOM | CAN_MCR_AWUM | CAN_MCR_TXFP`）；偶发错误帧能自动补发，不再丢关键命令。
  2. 新增 `motor_can_free_mailbox()`：返回空闲邮箱 0/1/2，全忙返回 3。
  3. `motor_can_send_frame()` 改为：等待 `MOTOR_CAN_TX_WAIT_TICKS`(5) 个 tick 且用 `vTaskDelay(1)` 让出 CPU → 仍满则 `CAN1->TSR |= (ABRQ0|ABRQ1|ABRQ2)` 中止三个 pending 邮箱（等价 `HAL_CAN_AbortTxRequest()`，已核对 HAL 源码用 `SET_BIT` 即 `|=`）→ 再查一次，仍无空闲邮箱则 `return 0` 放弃本帧，绝不无限等待。
  4. `bsp/boards/bsp_modbus.c` 增加 `#include "FreeRTOS.h"` 与 `#include "task.h"`（`main.h` 未传递包含）。
- **验证**：编译 0 错误 0 警告，FLASH 31.8 KB / RAM 27.9 KB。已确认无残留的无限 `while` 空转（`MX_CAN1_Init()` 里两处等 INAK 的循环、RX FIFO 排空循环均为启动期/有界）。
- **未做的增强（可选）**：发送失败计数上报（需在状态区开辟寄存器，贸然分配地址风险较大，暂未做）；`MX_CAN1_Init()` 中等待 INAK 的两处循环理论上也可加 `HAL_GetTick()` 超时。

### 待处理问题（尚未修改）

5. **使能帧与速度帧撞 ID**：两者都用 `0x201+i`，且使能寄存器非 0 时每 10ms 重复发使能帧。
6. **死代码**：`applications/remote_control.c`、`bsp/boards/bsp_rc.c` 未进任何 CMake target，从未编译。
7. **文档与代码脱节**：`readme.md` 写“尚未移植 FreeRTOS”、风机地址写 `0x0000`，与当前代码不符；`接口文档.md` 的寄存器映射同样过时。

### Git 记录

- 分支 `agents/credit-limit-inquiry`，三个提交已推送至 GitHub：
  - `04ca5f5` fix(modbus): 修正正交全向轮运动学解算的限幅符号与触发范围
  - `76c6ac2` fix(rtos): 将 HAL 时基迁移到 TIM6，为 FreeRTOS 提供 SysTick 节拍
  - `17285b2` chore: 删除遗留调试脚本，忽略 WorkBuddy 工作目录

