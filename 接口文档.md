# C 板风扇与电机接口文档

本文档用于说明 C 板与 EBS-P300 / ROS2 上层之间的 Modbus RTU 接口定义。目标是让上层系统通过标准 Modbus 区分访问两类设备：

- 风机：PWM 输出 + FG 反馈，属于吸附/散热/风压控制。
- 电机：CAN 控制 + 轮速/运动学输出，属于底盘驱动控制。

本项目中两者必须被视为两套完全不同的子系统：风机和电机不能混用同一套寄存器语义，也不能将电机地址覆盖到风机控制区。

## 1. 总体架构

- C 板：STM32F407 固件，作为 RS485 Modbus RTU 从站。
- EBS-P300 / ROS2：作为上层控制节点，读取风机状态、电机状态并发出控制命令。
- 物理层：RS485，默认 115200, 8N1。
- 默认从站地址：1。

推荐节点划分：

- `fan_bus_node`：Modbus RTU 串口通信、CRC 校验、地址过滤、请求/响应管理。
- `fan_state_node`：读取风机状态区寄存器，发布 RPM、占空比、脉冲计数与故障状态。
- `fan_cmd_node`：向风机控制区写入目标占空比，执行安全限幅和保护逻辑。
- `motor_bus_node`：维护 CAN 1 发送/接收，按 running 项目的协议驱动四个达妙电机。
- `motion_node`：将 vx / vy / wz 计算为四轮速度，写入电机控制区。
- `ebs_bridge_node`：连接 EBS-P300、汇总风机与底盘状态并做策略级联动。

## 2. 子系统隔离原则

### 2.1 风机子系统

- 风机 1~4 由 FG 反馈 + PWM 占空比控制。
- 风机 3/4 共用 PWM7，因此逻辑上仍然视为 4 个风机编号，但实际控制通道共有 3 路同步 PWM。
- 逻辑编号：1/2/3/4，硬件通道：1/5、2/6、3/7、4/7。
- 风机寄存器应保留在独立区，不应与 running 项目的电机控制区重叠。

### 2.2 电机子系统

- 电机为 CAN 总线驱动的 4 轮底盘驱动。
- 继续沿用 running 项目的 CAN 协议语义与寄存器布局。
- 电机地址不改，保证与 running 项目的上位机/驱动逻辑兼容。
- 风机寄存器如有冲突，优先后移风机地址，而不破坏电机地址。

## 2. 通信参数

| 项目 | 值 |
|------|----|
| 物理层 | RS485 |
| 波特率 | 115200 |
| 数据位 | 8 |
| 校验 | none |
| 停止位 | 1 |
| 从站地址 | 1（默认，可通过 0x0091 修改） |
| 功能码 | 0x03 / 0x04 / 0x06 / 0x10 |

## 3. Modbus 地址映射

> 说明：本文地址均为 Modbus 协议地址（0-based）。

### 3.1 风机子系统地址区

| 协议地址 | PLC 地址 | 名称 | 方向 | 类型 | 备注 |
|----------|----------|------|------|------|------|
| 0x0100 ~ 0x0103 | 40257 ~ 40260 | FANx_DUTY | 写 | uint16 | 风机 1~4 占空比，0~100 |
| 0x0064 ~ 0x008F | 40101 ~ 40140 | FAN_STATUS | 读 | uint16[] | 每风机 10 寄存器状态区 |
| 0x0090 | 40145 | UART_CFG | 读/写 | uint16 | 串口参数枚举 |
| 0x0091 | 40146 | SLAVE_ADDR | 读/写 | uint16 | 从站地址，默认 1 |
| 0x0092 ~ 0x00BF | 40147 ~ 40192 | USER_PARAM | 读/写 | uint16[] | 参数区 |
| 0x00C0 ~ 0x00FF | 40193 ~ 40256 | 保留区 | 读/写 | uint16[] | 预留 |

### 3.2 电机子系统地址区

| 协议地址 | PLC 地址 | 名称 | 方向 | 类型 | 备注 |
|----------|----------|------|------|------|------|
| 0x0000 ~ 0x0019 | 40001 ~ 40026 | MOTOR_CTRL | 读/写 | uint16[] | running 项目持续保留的电机控制区 |
| 0x0040 ~ 0x008F | 40065 ~ 40144 | MOTOR_STATUS | 读 | uint16[] | 电机状态区 |
| 0x0090 ~ 0x00BF | 40145 ~ 40192 | MOTOR_PARAM | 读/写 | uint16[] | 参数区，现统一保留给设备配置 |
| 0x00C0 ~ 0x00FF | 40193 ~ 40256 | 保留区 | 读/写 | uint16[] | 预留 |

说明：

- 风机和电机是两套不同的逻辑空间，不能合并在同一寄存器区。
- 本项目优先保证电机地址和 running 项目一致；若发生与风机地址冲突，则后移风机区而不改动电机区。

## 4. 风机控制区定义

风机控制区位于 0x0100 ~ 0x0103，用于写入四个风机的目标占空比。该区直接映射到风机 PWM 控制。

| 协议地址 | 名称 | 范围 | 含义 |
|----------|------|------|------|
| 0x0100 | FAN1_DUTY | 0~100 | 风机 1 目标占空比 |
| 0x0101 | FAN2_DUTY | 0~100 | 风机 2 目标占空比 |
| 0x0102 | FAN3_DUTY | 0~100 | 风机 3 目标占空比 |
| 0x0103 | FAN4_DUTY | 0~100 | 风机 4 目标占空比 |

说明：

- 取值范围为 0~100；超过 100 会被截断到 100。
- 0 表示停止。
- 这四个寄存器会同步写给 PWM5~PWM7 输出通道。
- 风机 3 和风机 4 共用 PWM7，因此两者的占空比控制需要在 ROS2/上位机侧保持一致。
- 风机控制区和电机控制区完全独立，地址不重叠。

## 5. 风机状态区定义

状态区位于 0x0040 ~ 0x008F，读该区域可获取各风机状态数据。状态区为只读，禁止 Modbus 写入。

### 5.1 风机状态区起始地址

| 风机 | 起始协议地址 | PLC 地址 |
|------|---------------|----------|
| 风机 1 | 0x0064 | 40101 ~ 40110 |
| 风机 2 | 0x006E | 40111 ~ 40120 |
| 风机 3 | 0x0078 | 40121 ~ 40130 |
| 风机 4 | 0x0082 | 40131 ~ 40140 |

### 5.2 单风机状态布局（10 个寄存器）

| 偏移 | 名称 | 类型 | 说明 |
|------|------|------|------|
| +0 | ERR | uint16 | 运行状态 / 故障码 |
| +1 | 保留 | uint16 | 固定为 0 |
| +2 / +3 | POS | float32 | 近似位置/脉冲累计 |
| +4 / +5 | VEL | float32 | 速度（RPM 近似） |
| +6 / +7 | T | float32 | 占空比/扭矩估计 |
| +8 | T_MOS | uint16 | MOS 温度估计 |
| +9 | T_Rotor | uint16 | 转子温度估计 |

说明：

- `POS`、`VEL`、`T` 使用 32 位浮点小端存储，布局为高字在前、低字在后。
- 当前状态区能反映四路 FG 反馈信息、转速估算与占空比反馈。
- ROS2 上层应将其转换成更高层的 `rpm`、`duty_cycle`、`pulse_count`、`fault` 等消息。

## 6. 电机控制区定义（对齐 running）

电机控制区保留 running 项目的寄存器布局，不进行改写，确保 CAN 4 轮控制协议兼容。

| 协议地址 | 名称 | 含义 |
|----------|------|------|
| 0x0000 ~ 0x0003 | MOTOR_EN | 4 个电机使能位 |
| 0x0004 ~ 0x0007 | MOTOR_CLR_ERR | 4 个电机清错命令 |
| 0x000A ~ 0x0011 | MOTOR_WHEEL_SPEED | 四轮目标速度寄存器 |
| 0x0014 ~ 0x0019 | VX_VY_WZ | 运动学输入参数 |
| 0x0064 ~ 0x008F | MOTOR_STATUS | 每电机状态区，按 10 寄存器布局保存 |

说明：

- 该段寄存器必须保持与 running 相同，不能因为风机区调整而破坏电机协议。
- `vx / vy / wz` 由上位机或运动学节点计算，再通过逆解算生成四个轮速。
- 发送到 CAN 1 时，控制目标仍按照 running 的电机 ID 201~204 处理。

## 7. 设备参数区定义

参数区位于 0x0090 ~ 0x00BF，支持读写，并具备 Flash 断电保持能力。

### 7.1 固定参数

| 协议地址 | PLC 地址 | 名称 | 类型 | 默认值 | 说明 |
|----------|----------|------|------|--------|------|
| 0x0090 | 40145 | UART_CFG | uint16 | 0 | 串口配置枚举 |
| 0x0091 | 40146 | SLAVE_ADDR | uint16 | 1 | Modbus 从站地址 |

说明：

- `SLAVE_ADDR` 允许范围为 1~247。
- 如果写入 0 或大于 247，代码会自动回退为 1。
- 实际站号生效方式：请求校验时使用当前参数中的站号。上位机需确保总线唯一站号。

### 7.2 断电保持

- 参数区使用 Flash A/B 双区交替写入。
- 每次写入参数区寄存器后，延迟约 2 秒再落盘，避免频繁 Flash 擦除。
- 启动时从 A/B 两个区中选择 seq 最大的有效数据。

## 8. ROS2 侧接口设计建议

### 8.1 读取风机状态

建议 ROS2 节点按以下形式读取状态：

- `Read Holding Registers`，起始地址 `0x0064`，数量 `0x2A`（42）
- 读取四个风机状态区共 40 个寄存器，再加上保留区补齐
- 每个风机按 10 寄存器解析，构造成统一状态消息

示例：

- fan1.status.err
- fan1.status.rpm
- fan1.status.pulse_count
- fan1.status.duty_cycle
- fan1.status.temperature_mos
- fan1.status.temperature_rotor

### 8.2 写入风机控制

建议 ROS2 节点按以下形式进行控制：

- `Write Multiple Registers` 或 `Write Single Register`
- 起始地址 `0x0100`
- 写入 4 个风机占空比寄存器

示例：

- 地址 0x0100 -> 写入 fan1 duty
- 地址 0x0101 -> 写入 fan2 duty
- 地址 0x0102 -> 写入 fan3 duty
- 地址 0x0103 -> 写入 fan4 duty

### 8.3 电机控制

- 电机控制区保留 running 项目寄存器布局，不做风机式重命名。
- 上位机可以通过 `vx / vy / wz` 写入电机运动学输入，并由控制节点进一步转换为四轮速度。
- CAN 发送对象仍保持 201、202、203、204 的电机 ID 语义。

## 9. Modbus 示例

### 9.1 读取风机 1 状态区

请求：

- 功能码：0x03
- 起始地址：0x0064
- 数量：0x000A

响应：

- 功能码：0x03
- 返回 10 个寄存器，解析为 `ERR`、`POS`、`VEL`、`T` 等字段

### 9.2 写入风机 1 占空比 60%

请求：

- 功能码：0x06
- 地址：0x0100
- 值：0x003C（60）

### 9.3 读取电机控制区

请求：

- 功能码：0x03
- 起始地址：0x0000
- 数量：0x001A

响应：

- 返回电机使能、清错和速度/运动学参数区的寄存器内容

## 10. 约束与建议

- ROS2 上层不要直接写状态区（0x0040~0x008F），该区仅供读取。
- 风机和电机必须分离处理，不共享寄存器语义。
- ROS2 上层应按风机编号维护 1/2/3/4 的映射，禁止混用 5/6/7 作为逻辑编号。
- 由于 PWM7 由风机 3 和风机 4 共用，控制上层应保证两者的占空比同步或按照统一策略处理。
- 电机区保持 running 项目布局，禁止将风机区覆盖到电机控制寄存器。
- 若需要扩展故障状态、温度阈值或保护动作，建议在参数区登记新增配置项。

## 11. 版本说明

当前文档与 C 板固件保持一致，适用于当前 RS485 Modbus 从站实现：

- 4 路 FG 反馈采集
- 3 路 PWM 输出同步
- 4 个风机占空比控制寄存器（0x0100~0x0103）
- 4 个风机状态区（0x0064~0x008F）
- 运行项目兼容的电机控制区（0x0000~0x0019）
- 参数区断电保持雏形

后续如果增加 CAN 设备通讯、FreeRTOS 任务化或完整运行项目寄存器映射，本文档应进一步补充对应字段。

参数区位于 0x0090 ~ 0x00BF，支持读写，并具备 Flash 断电保持能力。

### 6.1 固定参数

| 协议地址 | PLC 地址 | 名称 | 类型 | 默认值 | 说明 |
|----------|----------|------|------|--------|------|
| 0x0090 | 40145 | UART_CFG | uint16 | 0 | 串口配置枚举 |
| 0x0091 | 40146 | SLAVE_ADDR | uint16 | 1 | Modbus 从站地址 |

说明：

- `SLAVE_ADDR` 允许范围为 1~247。
- 如果写入 0 或大于 247，代码会自动回退为 1。
- 实际站号生效方式：请求校验时使用当前参数中的站号。上位机需确保总线唯一站号。

### 6.2 断电保持

- 参数区使用 Flash A/B 双区交替写入。
- 每次写入参数区寄存器后，延迟约 2 秒再落盘，避免频繁 Flash 擦除。
- 启动时从 A/B 两个区中选择 seq 最大的有效数据。

## 7. ROS2 侧接口设计建议

### 7.1 读取状态

建议 ROS2 节点按以下形式读取状态：

- `Read Holding Registers`，起始地址 `0x0040`，数量 `0x2A`（42）
- 读取四个风机状态区共 40 个寄存器，再加上保留区补齐
- 每个风机按 10 寄存器解析，构造成统一状态消息

示例：

- fan1.status.err
- fan1.status.rpm
- fan1.status.pulse_count
- fan1.status.duty_cycle
- fan1.status.temperature_mos
- fan1.status.temperature_rotor

### 7.2 写入控制

建议 ROS2 节点按以下形式进行控制：

- `Write Multiple Registers` 或 `Write Single Register`
- 起始地址 `0x0000`
- 写入 4 个风机占空比寄存器

示例：

- 地址 0x0000 -> 写入 fan1 duty
- 地址 0x0001 -> 写入 fan2 duty
- 地址 0x0002 -> 写入 fan3 duty
- 地址 0x0003 -> 写入 fan4 duty

### 7.3 参数配置

- 配置 `0x0091` 可调整从站地址。
- 配置 `0x0090` 可更换串口参数枚举值。
- 这些参数写入后建议重启设备使新参数生效。

## 8. Modbus 示例

### 8.1 读取 4 个风机占空比

请求：

- 功能码：0x03
- 起始地址：0x0000
- 数量：0x0004

响应：

- 功能码：0x03
- 数据长度：8 字节（4 个寄存器）
- 返回 4 个 16 位占空比值

### 8.2 读取风机 1 状态区

请求：

- 功能码：0x03
- 起始地址：0x0064
- 数量：0x000A

响应：

- 功能码：0x03
- 返回 10 个寄存器，解析为 `ERR`、`POS`、`VEL`、`T` 等字段

### 8.3 写入风机 1 占空比 60%

请求：

- 功能码：0x06
- 地址：0x0000
- 值：0x003C（60）

## 9. 约束与建议

- ROS2 上层不要直接写状态区（0x0040~0x008F），该区仅供读取。
- ROS2 上层应按风机编号维护 1/2/3/4 的映射，禁止混用 5/6/7 作为逻辑编号。
- 由于 PWM7 由风机 3 和风机 4 共用，控制上层应保证两者的占空比同步或按照统一策略处理。
- 若需要扩展故障状态、温度阈值或保护动作，建议在参数区登记新增配置项。

## 10. 版本说明

当前文档与 C 板固件保持一致，适用于当前 RS485 Modbus 从站实现：

- 4 路 FG 反馈采集
- 3 路 PWM 输出同步
- 4 个风机占空比控制寄存器
- 4 个风机状态区
- 参数区断电保持雏形

后续如果增加 CAN 设备通讯、FreeRTOS 任务化或完整运行项目寄存器映射，本文档应进一步补充对应字段。