# C 板 Modbus 接口文档（与代码一致）

> 本文档由 `bsp/boards/bsp_modbus.c` 中的 `REG_*` 宏自动核对，确保每一个地址/字段都与代码一致。寄存器命名沿用 running 项目（`D:\stm32\running\README.md` §3），是上层 Modbus 主机（EBS-P300 / ROS2）读写的唯一准绳。
>
> **变更历史（重要）**：
> - 早期版本将风机占空比放在 0x0000~0x0003；当前已迁移到 0x0100~0x0103。0x0000 段现为电机控制区，与电机 CAN ID 201~204 绑定。
> - CAN1 引脚由 PA11/PA12 改为 PD0/PD1（PD0=RX, PD1=TX, AF9）。
> - USB CDC 虚拟串口已下线（占用 PA11/PA12 的 USB_OTG_FS 与 CAN 冲突），固件体积从 56 KB 降到 31.7 KB。

## 1. 总体架构

- **C 板**：STM32F407 RoboMaster C 型开发板固件，作为 RS485 Modbus RTU 从站。
- **上层**：EBS-P300 / ROS2 节点作为 Modbus 主站，访问风机/电机两类子系统。
- **物理层**：RS485，默认 115200, 8N1。
- **从站地址**：默认 1，可由 `0x0091` 改为 1~247（写 0 或 >247 回退为 1）。

推荐节点划分（与 running 一致）：

- `fan_bus_node`：Modbus RTU 串口通信、CRC 校验、地址过滤、请求/响应管理。
- `fan_state_node`：读取风机状态区寄存器，发布 RPM、占空比、脉冲计数与故障状态。
- `fan_cmd_node`：向风机控制区（0x0100~0x0103）写入目标占空比，执行安全限幅和保护逻辑。
- `motor_bus_node`：维护 CAN 1 发送/接收，按 running 协议驱动四个达妙电机。
- `motion_node`：将 vx / vy / wz 计算为四轮速度，写入电机控制区（仅写 0x0014~0x0019）。
- `ebs_bridge_node`：连接 EBS-P300、汇总风机与底盘状态并做策略级联动。

## 2. 子系统隔离原则

C 板把寄存器空间划分成两套**互不重叠**的逻辑子系统：

| 子系统 | 控制区 | 状态区 | 总线 |
|--------|--------|--------|------|
| 电机 | 0x0000~0x0019 | 0x0040~0x008F | CAN 1 Mbps |
| 风机 | 0x0100~0x0103 | 0x0110~0x0137 | PWM/FG |

两套系统**不能合并到同一寄存器语义**，且风机地址**不能覆盖**电机控制区。发生冲突时优先后移风机地址，电机地址保持不变（沿用 running 协议）。

## 3. 通信参数

| 项目 | 值 |
|------|----|
| 物理层 | RS485（USART6, PG14/PG9, 方向 PG8）。接口 = 外壳丝印 UART1 的 3-Pin 口 |
| 波特率 | 115200, 8N1（运行期可通过 `0x0090` 改 115200/19200/9600 × N/E 7 种组合）|
| 从站地址 | 1（默认，可通过 `0x0091` 改 1~247）|
| 功能码 | 0x03 / 0x04 / 0x06 / 0x10 |
| 异常码 | 0x02 非法地址；0x04 设备故障（CRC 错误等）|

## 4. Modbus 地址映射总表

> 以下地址均为**协议地址（0-based）**。Modbus Poll 等主站工具通常使用 PLC 地址（1-based），即协议地址 0 对应 PLC 地址 40001。

| 协议地址 | PLC 地址 | 名称 | 读 | 写 | 断电保持 | 子系统 |
|----------|----------|------|----|----|----------|--------|
| 0x0000~0x0003 | 40001~40004 | 电机 1~4 使能 | 是 | 是 | 否 | 电机 |
| 0x0004~0x0007 | 40005~40008 | 电机 1~4 清错 | 是 | 是（边沿）| 否 | 电机 |
| 0x000A~0x0011 | 40011~40018 | 四轮目标角速度 | 是 | 否（被运动学覆盖）| 否 | 电机 |
| 0x0014~0x0015 | 40021~40022 | VY（float32，**前+**）| 是 | 是 | 否 | 电机 |
| 0x0016~0x0017 | 40023~40024 | VX（float32，**右+**）| 是 | 是 | 否 | 电机 |
| 0x0018~0x0019 | 40025~40026 | WZ（float32）| 是 | 是 | 否 | 电机 |
| 0x0040~0x008F | 40065~40144 | 电机状态区 | 是 | **否**（Modbus 写被忽略）| 否 | 电机 |
| 0x0090 | 40145 | UART_CFG | 是 | 是 | **是** | 参数 |
| 0x0091 | 40146 | SLAVE_ADDR | 是 | 是 | **是** | 参数 |
| 0x0092~0x00BF | 40147~40192 | 用户参数 | 是 | 是 | **是** | 参数 |
| 0x00C0~0x00FF | 40193~40256 | 保留区（沿用 running，留空）| 是 | 是 | 否 | 保留 |
| 0x0100~0x0103 | 40257~40260 | 风机 1~4 占空比 | 是 | 是 | 否 | 风机 |
| 0x0104~0x010F | 40261~40272 | 保留区 | 是 | 是 | 否 | 保留 |
| 0x0110~0x0119 | 40273~40282 | 风机 1 状态 | 是 | 否（每 5ms 由固件刷新）| 否 | 风机 |
| 0x011A~0x0123 | 40283~40292 | 风机 2 状态 | 是 | 否 | 否 | 风机 |
| 0x0124~0x012D | 40293~40302 | 风机 3 状态 | 是 | 否 | 否 | 风机 |
| 0x012E~0x0137 | 40303~40312 | 风机 4 状态 | 是 | 否 | 否 | 风机 |
| 0x0140~0x014F | 40321~40336 | **风机自动模式参数（姿态前馈）** | 是 | 是 | 否 | 风机 |
| 0x0150~0x015F | 40337~40352 | **IMU 姿态输出** | 是 | **否**（写被忽略）| 否 | IMU |

> 保持寄存器表总长为 **512**（0x0000~0x01FF）。早期版本为 256，导致风机占空比 0x0100 落在表外、
> 读写被边界检查静默丢弃；现已扩容，40257~40260 可正常访问。

## 5. 电机子系统（与 running 完全一致）

### 5.1 电机控制区

| 协议地址 | PLC 地址 | 名称 | 含义 |
|----------|----------|------|------|
| 0x0000~0x0003 | 40001~40004 | MOTOR_EN[1~4] | 写非 0=使能，写 0=失能；**上升沿/下降沿**触发一次使能/失能 CAN 命令 |
| 0x0004~0x0007 | 40005~40008 | MOTOR_CLR_ERR[1~4] | 写非 0=清除错误；**上升沿**（0→非 0）触发一次清错 CAN 命令 |
| 0x000A~0x0011 | 40011~40018 | MOTOR_WHEEL_SPEED[1~4] | 4 个 float（高字在低地址），单位 rad/s；**由运动学解算自动覆盖**，上位机不应直接写 |
| 0x0014~0x0015 | 40021~40022 | **VY** | float（高字在低地址），单位 m/s，**前+**（驱动电机 2/4）；**上位机写** |
| 0x0016~0x0017 | 40023~40024 | **VX** | float，单位 m/s，**右+**（驱动电机 1/3）；**上位机写** |
| 0x0018~0x0019 | 40025~40026 | **WZ** | float，单位 rad/s，**逆时针+** |

> ⚠️ **寄存器方向（与 `running/kinematics.c` 逐字一致，别按 ROS 直觉来）：**
> **`0x0014` = VY_HI（y，前+，驱动电机 2/4）**；**`0x0016` = VX_HI（x，右+，驱动电机 1/3）**；
> `0x0018` = WZ（逆时针+）。
>
> 顺序看着"反"（速度块从 VY 开始），但**不要交换** —— 这是与 `running` 一致的既有约定。
> （早期版本文档把 VY/VX 的方向写反了，现已更正。若你看到过"VY=左+ / VX=前+"的说法，那是错的。）

> **运动学解算**：ModbusTask 每次发速度前调用 `motor_kinematics_resolve()`，读 0x0014~0x0019，按正交全向轮逆运动学拆解成 4 轮角速度，写入 0x000A~0x0011，再发 CAN。上位机只需发车体速度 (vx, vy, wz)，不需自己拆 4 轮速。NaN/Inf 视为 0，轮速等比例限幅 **±50 rad/s**（`REG_MOTOR_W_MAX`，纯安全钳位）。
>
> **通信丢失保护**：上位机每次写 0x0014~0x0019 刷新心跳时间戳。若 500 ms 内无新命令（`KIN_LINK_TIMEOUT_MS`），强制 4 轮速度为 0，防止上位机崩溃/通信断开时小车失控。**上位机必须定期发送（建议 50 ms 一次）**，即使速度不变也要发，作为心跳。

### 5.2 电机状态区（只读）

每个电机占 **10 个寄存器**，起始地址如下：

| 电机 | 起始协议地址 | PLC 地址 | CAN ID |
|------|--------------|----------|--------|
| 电机 1 | 0x0064 (100) | 40101~40110 | 0x201 |
| 电机 2 | 0x006E (110) | 40111~40120 | 0x202 |
| 电机 3 | 0x0078 (120) | 40121~40130 | 0x203 |
| 电机 4 | 0x0082 (130) | 40131~40140 | 0x204 |

每电机 10 寄存器布局（相对起始偏移）：

| 偏移 | 名称 | 类型 | 说明 |
|------|------|------|------|
| +0 | ERR | uint16 | 状态码：0=失能，1=使能，5=传感器错误，6=电机参数错误，8=过压，9=欠压，A=过流，B=MOS 过温，C=线圈过温，D=通讯丢失，E=过载 |
| +1 | 保留 | — | 始终为 0 |
| +2 / +3 | POS | float (32 位) | 位置（高字在 +2，低字在 +3）|
| +4 / +5 | VEL | float (32 位) | 速度 |
| +6 / +7 | T   | float (32 位) | 扭矩 |
| +8 | T_MOS   | uint16 | 驱动 MOS 平均温度（℃）|
| +9 | T_Rotor | uint16 | 电机线圈平均温度（℃）|

> **定点缩放说明**：POS（16 位）、VEL/T（12 位）采用无符号定点线性映射（厂家 `uint_to_float`），公式 `f = x_int * span / ((1<<bits)-1) + x_min`，满量程分别为：POS ±12.5、VEL ±200.0、T ±10.0。解析后以 float 存入寄存器。

### 5.3 CAN 协议（PD0/PD1，AF9，1 Mbps）

- 波特率：1 Mbps，位时间 14 TQ（BRP=2，TS1=11TQ，TS2=2TQ，SJW=1TQ），采样点 85.7%。
- **自动重传开启**（`AutoRetransmission = ENABLE`），偶发错误帧自动补发；邮箱满时中止 pending 邮箱兜底。
- 收发走 **HAL API + `CAN1_RX0` 中断队列**：中断只入队，寄存器更新在任务上下文完成。

#### 5.3.1 发送（STM32 → 电机）

| 命令 | CAN ID | 字节内容 | 触发方式 |
|------|--------|----------|----------|
| 使能 | 0x201~0x204 | `FF FF FF FF FF FF FF FC` | D1~D4 由 0→非 0 上升沿 |
| 失能 | 0x201~0x204 | `FF FF FF FF FF FF FF FD` | D1~D4 由非 0→0 下降沿 |
| 清错 | 0x201~0x204 | `FF FF FF FF FF FF FF FB` | D5~D8 由 0→非 0 上升沿 |
| 速度 | 0x201~0x204 | 4 字节小端 float | 每 10 ms 发一次，依次给 4 个电机各发一帧 |

#### 5.3.2 接收（电机 → STM32）

- 电机状态帧 CAN ID = **0**（过滤器采用全 0 掩码，接收所有帧并由软件按 ID 分发）。
- 8 字节布局：

  | 字节 | 内容 |
  |------|------|
  | 0 | ID[3:0]（电机控制器 ID，1~4）\| ERR[7:4] |
  | 1 | POS[15:8] |
  | 2 | POS[7:0] |
  | 3 | VEL[11:4] |
  | 4 | VEL[3:0] \| T[11:8] |
  | 5 | T[7:0] |
  | 6 | T_MOS |
  | 7 | T_Rotor |

- POS / VEL / T 为无符号定点，经 `uint_to_float()` 线性映射为浮点：
  POS 16 位 → [-12.5, 12.5]，VEL 12 位 → [-200, 200]，T 12 位 → [-10, 10]（与 running 一致）。
- 解析后写入对应电机的状态寄存器（见 5.2）。

### 5.4 CAN 物理接口与接线（C 板）

开发板集成 2 路 CAN（依据官方手册「CAN 总线接口」节），**本工程只用 CAN1**：

| 接口 | 类型 | 引脚线序 | 本工程 |
|------|------|----------|--------|
| **CAN1** | **2-pin** | `1 = CANL` / `2 = CANH` | ✅ **用这个** |
| CAN2 | 4-pin | `1 = 5V` / `2 = GND` / `3 = CANH` / `4 = CANL` | ❌ 未使用 |

- MCU 引脚：**CAN1_TX = PD1、CAN1_RX = PD0（AF9）**；CAN2 = PB6/PB5。波特率 1 Mbps。
- **辨认方法：数脚的个数 —— 2-pin 那个就是 CAN1。** 不依赖外壳丝印。
  ⚠️ 手册**只**声明了 UART 的丝印错位（丝印 UART1 = USART6、丝印 UART2 = USART1），
  **CAN 没有这个问题**。
- 原厂 2-pin CAN 线：**黑 = CANL、红 = CANH**。达妙驱动器同为 H/L 两线，**H 接 H、L 接 L**。
  接反不会损坏但**完全不通信**，是此类问题最常见的接错。
- ⚠️ **2-pin CAN1 口没有 GND**。台面短线通常可用；长线/多节点建议从 4-pin CAN2 口的 GND
  或板上 GND 引参考地，并在总线两端各加 1 个 120 Ω 终端电阻（手册未提板载终端电阻）。

> ⚠️ **CAN 必须至少有一个节点 ACK。** 空总线下 `AutoRetransmission = ENABLE` 会让硬件
> 无限重传 → 3 个发送邮箱占满 → 固件走超时 + abort 逃生（每轮最多多阻塞约 20 ms）；
> 且 TEC 累加约 32 次即进入 **Bus-Off**，而 `AutoBusOff = DISABLE`（与 running 一致）
> **不会自愈**，CAN 控制器到下次复位前不再发送任何帧。
> → **要验证 CAN 物理层，必须挂一个会 ACK 的节点**（USB-CAN 分析仪 / 另一块 MCU / 一个电调）。

## 6. 风机子系统

### 6.1 风机控制区

| 协议地址 | PLC 地址 | 名称 | 范围 | 含义 |
|----------|----------|------|------|------|
| 0x0100 | 40257 | FAN1_DUTY | 0~100 | 风机 1 目标占空比 |
| 0x0101 | 40258 | FAN2_DUTY | 0~100 | 风机 2 目标占空比 |
| 0x0102 | 40259 | FAN3_DUTY | 0~100 | 风机 3 目标占空比 |
| 0x0103 | 40260 | FAN4_DUTY | 0~100 | 风机 4 目标占空比 |

说明：

- 取值范围 0~100，超过 100 会被截断到 100。
- 0 = 停止。
- 风机 1/2 单独对应 PWM5/PWM6；风机 3/4 共用 PWM7（取两者较大值）。
- 风机控制区和电机控制区完全独立，地址不重叠。

### 6.2 风机状态区

风机 1~4 各有 10 个状态寄存器，基址 0x0110 / 0x011A / 0x0124 / 0x012E（间隔 10），
由固件每 5ms 刷新，**上位机只读**（写入会被下一次刷新覆盖）。

| 偏移 | 协议地址（风机1）| 名称 | 类型 | 含义 |
|------|------------------|------|------|------|
| +0 | 0x0110 | FAN_RUN | uint16 | 运行标志：占空比 > 0 为 1，否则 0 |
| +1 | 0x0111 | — | uint16 | 保留，恒为 0 |
| +2/+3 | 0x0112~0x0113 | FAN_PULSE | float32 | FG 脉冲累计计数 |
| +4/+5 | 0x0114~0x0115 | FAN_RPM | float32 | 实测转速 RPM |
| +6/+7 | 0x0116~0x0117 | FAN_DUTY_FB | float32 | 当前输出占空比 0~100 |
| +8 | 0x0118 | FAN_T_MOS | uint16 | 估算 MOS 温度 |
| +9 | 0x0119 | FAN_T_ROTOR | uint16 | 估算转子温度 |

float32 采用与电机状态区相同的字序：**高字在前**（寄存器 0 = 高 16 位）。

> **本区每 5 ms 无条件刷新，与当前是手动还是自动模式无关。**
> 其中 `FAN_DUTY_FB`（+6/+7）取的是**实际送到定时器 CCR 的值**，而不是 0x0100~0x0103 的寄存器值 ——
> 自动模式下 0x0100~0x0103 只是手动备份值、不驱动输出，按寄存器回读会得到错误的反馈。
> 风机 3/4 共用 PWM7，两者反馈恒为同一值。

> 本区段是本工程后加的，与电机状态区（0x0064 起）**完全分离**。
> 早期实现曾把风机状态写进 0x0064/0x006E/0x0078/0x0082，占用了 running 的电机状态地址，现已纠正。

### 6.3 PWM/FG 资源映射（与硬件一致）

| 接口 | 定时器通道 | STM32 引脚 | 功能 | 对应风机 |
|------|------------|-----------|------|----------|
| PWM1 | TIM1_CH1 | PE9  | FG 输入捕获 | 风机 1 反馈 |
| PWM2 | TIM1_CH2 | PE11 | FG 输入捕获 | 风机 2 反馈 |
| PWM3 | TIM1_CH3 | PE13 | FG 输入捕获 | 风机 3 反馈 |
| PWM4 | TIM1_CH4 | PE14 | FG 输入捕获 | 风机 4 反馈 |
| PWM5 | TIM8_CH1 | PC6  | PWM 输出     | 风机 1 控制 |
| PWM6 | TIM8_CH2 | PI6  | PWM 输出     | 风机 2 控制 |
| PWM7 | TIM8_CH3 | PI7  | PWM 输出     | 风机 3 + 4 共用 |

### 6.4 风机自动模式（姿态前馈）：参数区 0x0140~0x014F

**控制律**（`fan_auto_update()`，在 ModbusTask 里每 5 ms 算一次）：

```text
cosθ = cos(pitch − PITCH_OFFSET) × cos(roll − ROLL_OFFSET)
duty = DUTY_FLAT + SLOPE_GAIN × (1 − cosθ)
duty = clamp(duty, DUTY_MIN, DUTY_MAX)     再钳到 100
```

- `pitch` / `roll` 取自 IMU 姿态快照（见 §6.6），单位度。
- 四路风机输出**同一个值**（不做分轴）：风机 1→PWM5、风机 2→PWM6、风机 3/4→PWM7。
- 这是**开环前馈**：roll/pitch 只用来"告知坡度"，不构成吸力闭环。

> **为什么用 (1 − cosθ) 而不是 θ？** 重力沿坡分量 ∝ sinθ，而"损失的法向吸附分量" ∝ (1−cosθ)，
> 后者才是要补的量。且 (1−cosθ) 在 θ=0 处导数为 0，水平附近天然平滑、不被姿态噪声放大；
> 值域有界于 [0,1]，增益的物理意义直观 ——
> **`SLOPE_GAIN` 就等于「竖直墙面（θ=90°）时额外增加的占空比百分点数」**。

| 协议地址 | PLC 地址 | 名称 | 类型 | 默认 | 说明 |
|----------|----------|------|------|------|------|
| 0x0140 | 40321 | FAN_MODE | uint16 | 0 | 0=手动（0x0100~0x0103 直接驱动）；1=自动 |
| 0x0141 | 40322 | FAN_AUTO_EN | uint16 | 0 | 自动总开关；**与 FAN_MODE 同时为 1 才接管输出** |
| 0x0142 | 40323 | DUTY_FLAT | uint16 | 30 | 水平姿态基准占空比（%）|
| 0x0143 | 40324 | DUTY_MIN | uint16 | 30 | 自动输出下限（%），**必须 > 0** |
| 0x0144 | 40325 | DUTY_MAX | uint16 | 100 | 自动输出上限（%）|
| 0x0145 | 40326 | SLOPE_GAIN | uint16 | 60 | 坡度增益（% 每单位 (1−cosθ)）|
| 0x0146 | 40327 | KP_PITCH | uint16 | 0 | 预留，当前实现未使用 |
| 0x0147 | 40328 | KP_ROLL | uint16 | 0 | 预留，当前实现未使用 |
| 0x0148 | 40329 | PITCH_OFFSET | int16 | 0 | 俯仰零位偏置（**整数度**）|
| 0x0149 | 40330 | ROLL_OFFSET | int16 | 0 | 横滚零位偏置（**整数度**）|
| 0x014A | 40331 | MAG_ENABLE | uint16 | 0 | 磁力计使能，本轮恒 0（只读，写被忽略）|
| 0x014B | 40332 | MAG_STATUS | uint16 | 2 | 磁力计状态，恒 2=未接入（只读）|
| 0x014C | 40333 | MAG_CAL_CMD | uint16 | 0 | 保留（只读，写被忽略）|
| 0x014D~0x014E | 40334~40335 | HEATER_TARGET | float32 | 45.0 | 恒温目标温度（℃，高字在 0x014D）；写入即调用 `ins_set_target_temp()`，限 20~70℃ |
| 0x014F | 40336 | HEATER_PWM | uint16 | 只读 | 诊断：当前加热 PWM 输出 0~4500 |

> **`DUTY_MIN` 必须 > 0（默认 30）。** 重力补偿的物理前提是"吸附力始终存在"：
> 一旦占空比掉到 0，风机停转、吸附力消失，小车直接脱离壁面。所以即使 θ=0、`1−cosθ`=0，
> 输出也不会低于 `DUTY_MIN`。

> **手动 / 自动互斥**：自动模式（0x0140=1 且 0x0141=1）下，写 0x0100~0x0103
> **只保存不驱动**（输出被 `fan_auto_update()` 每 5 ms 覆盖）；把 0x0140 写回 0 后，
> 之前保存的手动值**立即恢复生效**，无需重写。这样标定时手动值与姿态值不会互相打架。

### 6.5 倾斜角标定流程

**标定目标：求出 `DUTY_FLAT` 与 `SLOPE_GAIN` 两个系数**（其余用默认即可）。

#### 第 0 步 · 零位偏置（必须最先做，否则后面全偏）

小车放**水平**台面静止，读 0x0150(ROLL) / 0x0152(PITCH)：

| 读数 | 动作 |
|------|------|
| 都在 0（±0.3° 内）| 跳过，偏置保持 0 |
| 例如 PITCH=+1.5°、ROLL=−0.8° | 写 **0x0148 = 2**、**0x0149 = −1** |

> 偏置是 **int16、单位为整数度**（代码为 `snap.pitch − (float)(int16_t)offset`），
> 所以只补到 1° 精度、残差 ±0.5°，对开环前馈足够。
> **符号：直接填"水平时的读数"，不用取负** —— 代码是做减法。

#### 第 1 步 · 切手动

写 0x0140 = 0（复位后默认就是手动）。此时 0x0100~0x0103 直接驱动风机输出。

#### 第 2 步 · 逐角度采点（核心）

在**真实工况面**（风洞斜面）上放车，对每个倾角拿一个数据点：

1. 读 0x0150 / 0x0152，**用这两个值作为实际倾角**（不要用你量角器读的数）
2. 给 0x0100~0x0103 写同一个占空比，**从低往高试**
3. 找到**临界吸附点**：再降一点小车就开始下滑的那个占空比
4. 记录 `(θᵢ, dutyᵢ)`

建议取 **4~5 个点**，覆盖 0° / 15° / 30° / 45° / 60°。

`1−cosθ` 查表（拟合时直接用）：

| θ | 0° | 10° | 15° | 20° | 30° | 45° | 60° | 75° | 90° |
|---|----|-----|-----|-----|-----|-----|-----|-----|-----|
| 1−cosθ | 0 | 0.0152 | 0.0341 | 0.0603 | 0.1340 | 0.2929 | 0.5000 | 0.7412 | 1.0000 |

#### 第 3 步 · 拟合两个系数

**两点法**（取首尾两点最省事）：

```text
SLOPE_GAIN = (d_last − d_first) / ((1−cosθ_last) − (1−cosθ_first))
DUTY_FLAT  = d_first − SLOPE_GAIN × (1−cosθ_first)
```

**多点最小二乘**（更稳，推荐）。令 `xᵢ = 1−cosθᵢ`、`yᵢ = dutyᵢ`：

```text
SLOPE_GAIN = Σ(xᵢ−x̄)(yᵢ−ȳ) / Σ(xᵢ−x̄)²
DUTY_FLAT  = ȳ − SLOPE_GAIN × x̄
```

*例*：0° 时临界 32%，60° 时临界 62%
→ `SLOPE_GAIN = (62−32)/(0.5−0) = 60`，`DUTY_FLAT = 32`
→ 写 0x0142 = 32、0x0145 = 60；竖直墙面时输出 `32 + 60 = 92%`。

#### 第 4 步 · 写回参数

| 写什么 | 地址 | 取值建议 |
|--------|------|----------|
| 基准占空比 | 0x0142 | 拟合出的 DUTY_FLAT |
| 坡度增益 | 0x0145 | 拟合出的 SLOPE_GAIN |
| 输出下限 | 0x0143 | **必须 ≥ 30**；建议 `max(30, DUTY_FLAT − 5)` |
| 输出上限 | 0x0144 | 100（或风机能承受的上限）|

> ⚠️ **这些参数不在断电保持区**（保持区只有 0x0090~0x00BF），**复位即丢失**。
> 需要每次上电重写，或在上位机启动脚本里固定写一遍。

#### 第 5 步 · 切自动并验证

1. 写 0x0140 = 1、0x0141 = 1
2. 把小车**缓慢倾斜**，盯 **0x015C(AUTO_DUTY)**：应随倾角单调上升，且与公式对得上
3. 上风洞实测，按"贴不住就加 SLOPE_GAIN、贴得住但吸力过剩就减"微调

> **风机状态反馈（0x0110~0x0137）在自动/手动两种模式下都会刷新**（每 5 ms 无条件刷新）。
> 其中占空比反馈取的是**实际送到定时器 CCR 的值**，不是 0x0100~0x0103 的寄存器值 ——
> 所以自动模式下读到的是控制律算出的**实时输出**。
> 风机 3/4 共用 PWM7，因此 `0x0124~0x012D` 与 `0x012E~0x0137` 两块的占空比反馈恒为同一个值，属正常。

### 6.6 IMU 姿态输出区 0x0150~0x015F（只读）

由 `refresh_imu_registers()` 每 5 ms 刷新，**上位机只读**（写入被静默忽略）。
float32 一律**高字在前**（低地址 = 高 16 位）。

| 协议地址 | PLC 地址 | 名称 | 类型 | 单位 | 说明 |
|----------|----------|------|------|------|------|
| 0x0150~0x0151 | 40337~40338 | ROLL | float32 | deg | 横滚角，右倾为正 |
| 0x0152~0x0153 | 40339~40340 | PITCH | float32 | deg | 俯仰角，抬头为正 |
| 0x0154~0x0155 | 40341~40342 | YAW | float32 | deg | 偏航角，逆时针为正（**会漂，见下**）|
| 0x0156~0x0157 | 40343~40344 | GYRO_X | float32 | rad/s | 绕 X 角速度 |
| 0x0158~0x0159 | 40345~40346 | GYRO_Y | float32 | rad/s | 绕 Y 角速度 |
| 0x015A~0x015B | 40347~40348 | GYRO_Z | float32 | rad/s | 绕 Z 角速度 |
| 0x015C~0x015D | 40349~40350 | AUTO_DUTY | float32 | % | 自动模式当前输出占空比（手动模式恒 0）|
| 0x015E | 40351 | IMU_STATUS | uint16 | — | **整型！** 0=离线 1=加热中 2=运行 3=错误 |
| 0x015F | 40352 | TEMP_X10 | int16 | ℃×10 | BMI088 温度，如 343 = 34.3℃ |

> **YAW 持续漂移是预期行为，不是故障。** 本工程只用六轴（陀螺 + 加速度计），无磁力计，
> yaw 方向没有绝对参考，数学上退化为 `yaw(t) = yaw(0) + ∫gyro_z dt`。
> 陀螺零偏约 0.18~0.24 °/s → 漂 ~11 °/分钟、~650 °/小时，并在 **±180° 处回绕**（锯齿波），
> 取值恒在 **(−180°, +180°]**。**风机前馈只用 roll/pitch，不依赖 yaw。**
> 要根治需接板载 IST8310（I2C3: PA8/PC9、RST PG6、DRDY PG3）并改用九轴融合。

> **Modbus Poll 显示提示**：这些是 float32，需把数据格格式设为 `Float (ABCD)`（高字在前）。
> 每个 float 占两行，**第二行显示 `--`（那是低字，不是独立量）**。
> 0x015E 是**整型**，被当 float 解析会显示 `--`，需单独把那一格改回 `Signed`。

## 7. 参数区（断电保持）

参数区位于 0x0090~0x00BF，支持读写，并具备 Flash A/B 双区断电保持能力。

### 7.1 固定配置寄存器

| 协议地址 | PLC 地址 | 名称 | 类型 | 默认值 | 说明 |
|----------|----------|------|------|--------|------|
| 0x0090 | 40145 | UART_CFG | uint16 | 0 | 串口参数枚举（见 7.2）|
| 0x0091 | 40146 | SLAVE_ADDR | uint16 | 1 | Modbus 从站地址，1~247；写 0 或 >247 回退为 1 |
| 0x0092~0x00BF | 40147~40192 | USER_PARAM | uint16[] | — | 用户参数，带断电保持 |

### 7.2 UART_CFG 枚举值（寄存器 0x0090）

| 值 | 波特率 | 数据位 | 校验 | 停止位 |
|----|--------|--------|------|--------|
| 0（默认）| 115200 | 8 | None | 1 |
| 1 | 115200 | 8 | Even | 1 |
| 2 | 19200  | 8 | None | 1 |
| 3 | 19200  | 8 | Even | 1 |
| 4 | 9600   | 8 | None | 1 |
| 5 | 9600   | 8 | Even | 1 |
| 6 | 38400  | 8 | None | 1 |
| 7 | 57600  | 8 | None | 1 |

> 校验位非 None 时，HAL 侧数据位口径为"含校验位共 9 位"（即实际数据 8 位 + 1 位校验），
> 与 running 一致。越界值（≥8）一律回退到 0（115200 8N1）。

> **注意**：`UART_CFG` 和 `SLAVE_ADDR` 在**上电启动时读取一次**，运行期写入并保存到 Flash 后，需**复位重启**才会以新的波特率/站号生效。

### 7.3 断电保持策略

- 参数区使用 Flash A/B 双区交替写入。起始地址：A 区 0x080C0000（Sector 10），B 区 0x080E0000（Sector 11），每区 128 KB。
- 写入采用 A/B 交替策略，每次写另一区，带 magic/version/seq/CRC32 校验。
- 参数区任意寄存器被 Modbus 写入后，延迟 **2 秒**去抖才落盘（避免连续写频繁擦除 Flash）。
- 启动时从 A/B 两区中选择 seq 最大的有效镜像。
- 程序 Flash 占用 0x08000000~0x080BFFFF（768 KB），与保持区无重叠。

## 8. ROS2 / 上位机集成示例

### 8.1 读取电机状态（推荐）

- 功能码 `0x03`，起始地址 `0x0064`，数量 `0x0028`（40）
- 一次读 4 个电机状态区共 40 个寄存器
- 每块按 10 寄存器解析：ERR / reserved / POS(float) / VEL(float) / T(float) / T_MOS / T_Rotor

```text
fan_state_node（或 motor_state_node）示例：
  40101~40110  -> motor 1 status
  40111~40120  -> motor 2 status
  40121~40130  -> motor 3 status
  40131~40140  -> motor 4 status
```

### 8.2 下发车体速度（推荐）

- 功能码 `0x10`，起始地址 `0x0014`（即 PLC 40021），数量 `0x0006`（6 个寄存器）
- 依次为 VY 高/低字、VX 高/低字、WZ 高/低字
- 例：前进 0.5 m/s，零旋转：写 6 个寄存器 = `0x3F00 0x0000 0x0000 0x0000 0x0000 0x0000`（0.5f 高字 0x3F00 在 40021）
- **每 50 ms 写一次**作为心跳，否则 500 ms 后会被强制停车

### 8.3 控制风机占空比

- 功能码 `0x10`，起始地址 `0x0100`（即 PLC 40257），数量 `0x0004`（4 个寄存器）
- 写 4 个 uint16 占空比 0~100

```text
示例：4 个风机各 60% 占空比
  0x0100 <- 0x003C (60)
  0x0101 <- 0x003C (60)
  0x0102 <- 0x003C (60)
  0x0103 <- 0x003C (60)
```

### 8.4 使能电机 1

- 功能码 `0x06`，地址 `0x0000`（即 PLC 40001）
- 先写 0 再写 1：第二次写入的 0→1 上升沿触发使能 CAN 命令 `FF...FC`

### 8.5 读取 IMU 姿态 + 温度

- 功能码 `0x03`，起始地址 `0x0150`（PLC 40337），数量 `0x0010`（16）
- 一次读完 roll/pitch/yaw/gyro/auto_duty/status/temp

```text
40337~40338  ROLL        float32 (ABCD, 高字在前)
40339~40340  PITCH       float32
40341~40342  YAW         float32   ← 会漂，正常
40343~40344  GYRO_X      float32
40345~40346  GYRO_Y      float32
40347~40348  GYRO_Z      float32
40349~40350  AUTO_DUTY   float32   %  自动模式当前输出
40351        IMU_STATUS  uint16    2 = 运行
40352        TEMP_X10    int16     ℃×10
```

> **最小可用集**：只想判"IMU 活着吗"就只读 `0x015E`（=2 表示运行）和 `0x015F`（非 0 表示温度正常）。
> 注意这两个是**整型**，别按 float 解析。

### 8.6 切入风机自动模式（姿态前馈）

标准三步（参数用默认，或先按 §6.5 标定后写回）：

```text
1) 写标定参数（可选，按 §6.5 拟合结果）
   0x0142 <- DUTY_FLAT      例 32
   0x0145 <- SLOPE_GAIN     例 60
   0x0143 <- DUTY_MIN       必须 >= 30
   0x0144 <- DUTY_MAX       100

2) 写零位偏置（若水平时 roll/pitch 不为 0）
   0x0148 <- PITCH_OFFSET   整数度
   0x0149 <- ROLL_OFFSET    整数度

3) 切自动
   0x0140 <- 1   (FAN_MODE)
   0x0141 <- 1   (FAN_AUTO_EN)
```

验证：缓慢倾斜小车，读 `0x015C`(AUTO_DUTY) 应随倾角上升，例如 θ=0° 时约 32%、
θ=60° 时约 62%。切回手动只需 `0x0140 <- 0`，之前 0x0100~0x0103 的手动值立即恢复。

### 8.7 标定用的手动临界占空比

标定阶段（`0x0140` = 0）直接写 0x0100~0x0103，与 §8.3 相同：

```text
功能码 0x10，起始 0x0100，数量 4
  0x0100~0x0103 <- 同一个值（四路同值）
逐档上调，找到"再低一点就下滑"的临界值。
```

## 9. 约束与建议

- **不要写状态区** 0x0040~0x008F：Modbus 写入会被静默忽略。
- **不要写轮速** 0x000A~0x0011：会被运动学解算结果覆盖（每 10 ms）。只需发 0x0014~0x0019。
- **风机和电机必须分离处理**，不共享寄存器语义。
- ROS2 节点按风机编号 1/2/3/4 维护，禁止混用 5/6/7 作为逻辑编号。
- 风机 3 和 4 共用 PWM7，建议上层把两者的目标占空比保持一致。
- 修改站号/波特率（`0x0090` / `0x0091`）后需等待 2 s 落盘 + 复位重启才生效。
- 旧上位机若按 0x0000~0x0003 读"风机占空比"，会读到电机使能位，请按本文档 §6.1 改为 0x0100~0x0103。
- **不要写 IMU 区** 0x0150~0x015F：只读，写入被静默忽略。
- **自动模式下不要指望 0x0100~0x0103 生效**：它只作手动备份值保存，输出由 0x0140~0x0145 的控制律接管。
- **`DUTY_MIN` 永远不要设成 0**：吸附力归零 = 小车掉壁。
- **自动模式参数（0x0140~0x014F）不在断电保持区**，复位即恢复默认值（FAN_MODE=0 手动、DUTY_FLAT=30、
  SLOPE_GAIN=60）。要开机即用自动，需在上位机启动脚本里重写一遍。
- 自动模式下 0x0110~0x0137 风机状态**照常刷新**（每 5 ms），占空比反馈 = 实际输出。
- 风机 3/4 共用 PWM7，`0x0124~0x012D` 与 `0x012E~0x0137` 的占空比反馈恒为同一值，属正常。

## 10. 与 running 项目的兼容性

电机子系统（§5）的寄存器布局、Modbus 协议语义、CAN 帧格式、状态帧字节解析、UART_CFG 枚举、断电保持策略等均与 `D:\stm32\running\README.md` §3 保持一致，可直接复用 running 的 Modbus 上位机和 ROS2 节点。

风机子系统（§6）是 C 板本体的扩展，不在 running 项目中；地址 0x0100~0x0103 是为避开电机区而在 C 板上新设的。
姿态前馈自动模式（§6.4~6.5）与 IMU 姿态输出区（§6.6）同样是 C 板扩展，running 无对应实现；
但 `running` 的 `RM_C_Board_Template/Drivers/BSP` 下已有 BMI088 + Mahony 参考实现，融合算法即移植自它。
